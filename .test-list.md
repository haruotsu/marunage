# PR-102 reflection hook — t_wada test list

> Drive Red → Green → Refactor in this order. Tick each entry when the
> matching Go test goes green. New cases discovered mid-implementation get
> appended at the bottom of the relevant section.

## A. internal/store SetReflection

- [x] R1. `SetReflection` writes reflection text to an existing row; other columns untouched.
- [x] R2. `SetReflection` on a missing id returns `ErrNotFound`.
- [x] R3. `SetReflection` accepts an empty string (clears the column).
- [x] R4 (post-design-review): `SetReflection` rejects non-terminal states with `ErrInvalidTransition` so a `marunage reopen` race cannot pin a stale reflection onto a re-pending row.

## B. internal/dispatch reflection hook (reflection.go)

- [x] N1. `New` fails `ErrInvalidConfig` when `WithStore` is omitted.
- [x] N2. `New` fails `ErrInvalidConfig` when `WithCmux` is omitted.
- [x] N3. `New` fails `ErrInvalidConfig` when `WithSkill` is empty.
- [x] N4. `New` fails `ErrInvalidConfig` when `WithWorkspaceDirs` is omitted.
- [x] N5. `New` rejects `WithSampleRate` outside [0,1].
- [x] N6. `New` defaults timeout to 5m, clock to time.Now, sample_rate to 1.0, auditor to NopAuditor.

## C. Sampling

- [x] S1. `WithSampleRate(0)` => OnDone never sends.
- [x] S2. `WithSampleRate(1)` => OnDone always sends. (covered by H3 via default rate=1.0 + H7 fan-out.)
- [x] S3. `WithSampler` overrides the rate-based sampler. (last-writer-wins.)

## D. Async hook behaviour (Reflector.OnDone + Wait)

- [x] H1. `OnDone` with empty `task.WS` is a no-op (no Send, no SetReflection, no goroutine started).
- [x] H2. `OnDone` with sampler.False is a no-op.
- [x] H3. `OnDone` calls `cmux.Send(ws, prompt)` with prompt containing the skill body and the reflection-sentinel write instruction.
- [x] H4. After `Send`, the goroutine polls for `<workspaceDir>/.reflection` and persists the trimmed contents via `store.SetReflection`.
- [x] H5. Timeout: when `.reflection` never appears, the goroutine exits before the parent ctx fires; no SetReflection call; audit `reflection.timeout` recorded.
- [x] H6. Parent ctx cancel propagates to the goroutine; it exits without calling SetReflection; audit `reflection.cancel` recorded.
- [x] H7. `Wait` blocks until every dispatched goroutine exits.
- [x] H8. `cmux.Send` failure: audit `reflection.fail`; no SetReflection call.
- [x] H9. Audit `reflection.start` recorded with `Key=task:<id>`, `Value=<ws>` immediately before the Send.
- [x] H10 (post-design-review): orphan `.reflection.tmp` (mid-publish) does not trigger SetReflection — the polled file is the post-rename `.reflection`, so half-written reads cannot land in `tasks.reflection`.
- [x] H11 (post-design-review): the prompt body uses a heredoc rather than `printf '%s'` so a reflection containing `%`, `"`, `\`, or newlines round-trips verbatim. (covered by H3 string-shape assertion.)
- [x] H12 (review-fix-loop iter1): `cmux.Send` returning `context.Canceled` is classified as `reflection.cancel`, not `reflection.fail`.
- [x] H13 (review-fix-loop iter1): `cmux.Send` returning `context.DeadlineExceeded` is classified as `reflection.timeout`.
- [x] H14 (review-fix-loop iter1): audit Value runs through `logging.Redact` so a `Bearer sk-…` echoed back from cmux stderr does not pin to audit.log.
- [x] H15 (review-fix-loop iter1): the prompt advertises the configured `WithReflectionTimeout`, not the package default.

## E. completion package WithDoneHook integration

- [x] C1. `completion.WithDoneHook(fn)` invokes fn with the task's id immediately after a successful done transition.
- [x] C2. Hook is NOT invoked when MarkDoneWithSummary fails.
- [x] C3. Hook is NOT invoked on the failed (non-zero exit) branch.
- [x] C4. Hook receives the post-done store.Task (so reflection prompt can carry the result_summary if needed). (covered by C1: status/summary/completed_at asserted on the captured task.)

---

# PR-43 atomic sentinel completion detection — t_wada test list

> Drive Red → Green → Refactor in this order. Tick each entry when the matching
> Go test goes green. New cases discovered mid-implementation get appended at the
> bottom of the relevant section.

## A. internal/store additions (atomic done + completed_at)

- [x] S1. `MarkDoneWithSummary` on a row stamps status=done, result_summary=<text>, completed_at=<time>.
- [x] S2. `MarkDoneWithSummary` on a missing id returns `ErrNotFound`.
- [x] S3. `MarkDoneWithSummary` accepts an empty summary (Claude may produce no final text).
- [x] S4. `MarkDoneWithSummary` with zero `time.Time` returns `ErrCompletedAtRequired`.
- [x] S5. `SetCompletedAt` stamps completed_at on a row; missing id returns `ErrNotFound`; zero time returns `ErrCompletedAtRequired`.
- [x] S6. `MarkDoneWithSummary` writes the canonical millisecond ISO8601 layout that `Get` round-trips.

## B. internal/completion package — Watcher.Tick(ctx)

- [x] W1. No running rows → `Tick` returns nil and never touches the filesystem.
- [x] W2. Running row without sentinel → `Tick` is a no-op (row stays running, no audit, no store writes).
- [x] W3. Running row with sentinel `0\n` → row marked done, audit `completion.detect` recorded carrying the task id.
- [x] W4. Running row with sentinel `1\n` (non-zero exit) → row marked failed, judgment_reason mentions `exit_code=1`, audit `completion.fail`.
- [x] W5. Running row with sentinel `garbage` (non-numeric) → row marked failed, judgment_reason mentions parse failure, audit `completion.fail`.
- [x] W6. `.exit_code.tmp` present but `.exit_code` absent → `Tick` is a no-op (atomicity preserved; reader does not consume partial file).
- [x] W7. `.result_summary` file present alongside `.exit_code` → its trimmed contents become `tasks.result_summary`.
- [x] W8. `.result_summary` absent on success → result_summary stays empty.
- [x] W9. Multiple running rows: each one independently checked; one's sentinel does not affect another's outcome.
- [x] W10. Workspace directory missing entirely → row stays running (PR-44 reaper's job, not ours; no audit fired).
- [x] W11. Audit `completion.detect` carries `Key="task:<id>"` and `Value="<exit_code>"`.
- [x] W12. Audit `completion.fail` carries `Key="task:<id>"` and `Value` containing the failure reason.
- [x] W13. Sentinel write is atomic for the reader: simulating the documented `mv .exit_code.tmp .exit_code` (write tmp then rename) leaves the watcher seeing only the final byte once, never a half-written file.

## C. Watcher construction

- [x] N1. `New` fails with `ErrInvalidConfig` when `WithStore` is omitted.
- [x] N2. `New` fails with `ErrInvalidConfig` when `WithWorkspaceDirs` is omitted.
- [x] N3. `New` defaults clock to `time.Now`, auditor to `config.NopAuditor`, poll interval to 5s.

## D. Watcher.Run(ctx) — long-running loop

- [x] R1. `Run` invokes `Tick` once per poll interval and continues until ctx is cancelled.
- [x] R2. ctx cancellation returns nil so the daemon caller can shut down cleanly.
- [x] R3. `Run` calls `Tick` immediately on entry (does not wait the first poll interval).

## E. Prompt wiring (internal/dispatch/prompt.go)

- [x] P1. `BuildPrompt` with non-empty `WorkspaceDir` appends a sentinel-write instruction containing the absolute `.exit_code` path.
- [x] P2. The sentinel instruction includes the documented atomic command pair: `echo $? > .exit_code.tmp` then `mv .exit_code.tmp .exit_code` (so a reader never sees a half-written file).
- [x] P3. Empty `WorkspaceDir` omits the sentinel instruction (back-compat for callers that have not wired completion yet).
- [x] P4. The instruction tells Claude to write `.result_summary` (trimmed final summary) before the sentinel rename so Watcher reads it atomically.

## F. Dispatcher wiring (internal/dispatch/dispatch.go)

- [x] F1. `WithWorkspaceDirs` plumbs the same `Dirs` provider used by Watcher; the dispatcher mkdirs the per-task dir before NewWorkspace and passes the dir into `BuildPrompt`.
- [x] F2. Mkdir failure leaves the row pending (do not flip to failed; PR-43 wants retry semantics consistent with NewWorkspace failure).
- [x] F3. Default behaviour with no `WithWorkspaceDirs` keeps PR-42 wire-format intact (no sentinel section, no mkdir).

---

# PR-42b test list (t_wada TDD) [merged history]

Each item is one Red → Green → (Refactor) cycle. Tick `[x]` once the matching test is green AND any prior tests still pass.

## F. workspaceName UTF-8 (rune-based trim)

- [x] F1. Title with `titleMaxLen+1` runes worth of emoji is trimmed to `titleMaxLen` runes (no garbled bytes, output is valid UTF-8).
- [x] F2. Title with Japanese characters exceeding `titleMaxLen` runes is trimmed to `titleMaxLen` runes (no garbled bytes).
- [x] F3. Pure ASCII title beyond `titleMaxLen` still uses `titleMaxLen` runes (regression guard for the existing behaviour).

## G. Prompt injection fences (BuildPrompt)

- [x] G1. BuildPrompt wraps `Source`, `ExternalID`, `ExternalURL`, `Title`, `Body` in `<<source>>…<</source>>` etc. fences.
- [x] G2. BuildPrompt escapes any literal `<<` substring that appears inside any user-derived field, so an attacker cannot forge a fence-close.
- [x] G3. Empty `ExternalID` / `ExternalURL` produce empty fenced sections (or are omitted) without doubled blank lines (`\n\n\n\n` regression).
- [x] G4. Trusted sections `Base` and `SourceSpecific` are NOT rewritten / fenced (they originate from skills/, not user input).
- [x] G5. Existing tests (TestBuildPromptOrdersSections / TestBuildPromptHandlesEmptySourceSection / TestBuildPromptIncludesTaskMetadata) still pass.

## H. Token redaction

- [x] H1. `Redact("error: token sk-ant-abcdefghijklmnopqrstuv leaked")` returns the same string with the secret replaced by `[REDACTED]`.
- [x] H2. `Redact` masks GitHub PATs (`ghp_…`, `gho_…`, `ghu_…`, `ghs_…`, `ghr_…`).
- [x] H3. `Redact` masks `Bearer <token>` and `Authorization: Bearer <token>` (case insensitive).
- [x] H4. `Redact` masks Slack tokens (`xoxb-…`, `xoxp-…`, `xoxa-…`, `xoxr-…`, `xoxs-…`).
- [x] H5. `Redact` leaves non-secret text untouched.
- [x] H6. Dispatcher's `markFailed` redacts the dispatch reason BEFORE writing to `judgment_reason` and BEFORE recording the `dispatch.fail` audit event.
- [x] H7. `RedactingHandler` wraps a slog.Handler so the log line message AND attribute values are redacted on the way out.

## I. permission.Matcher wiring + on_unknown_permission

- [x] I1. With Matcher configured + tool/args on the allowlist, `Dispatcher.HandlePermissionRequest` returns `PermissionAllow` and does not touch the row.
- [x] I2. With Matcher denying + policy=`escalate`, `HandlePermissionRequest` returns `PermissionEscalate`, calls `store.EscalateToHuman(id, reason)`, and the reason names tool/args.
- [x] I3. With Matcher denying + policy=`fail`, `HandlePermissionRequest` returns `PermissionFail` and calls `store.MarkFailedWithReason(id, reason)`.
- [x] I4. With Matcher denying + policy=`retry`, `HandlePermissionRequest` returns `PermissionAsk` (defer the decision back to the caller / Claude protocol; no row mutation).
- [x] I5. With no Matcher configured, `HandlePermissionRequest` returns `PermissionAsk` (safe default — never silently allow).
- [x] I6. Escalate branch records a `dispatch.escalate` audit event with the task id + tool/args reason.
- [x] I7. `New(...)` accepts `WithPermissionMatcher` + `WithOnUnknownPermission`; an unknown policy string fails construction with `ErrInvalidConfig`.
- [x] I8. The reason string written to judgment_reason / audit / etc. is passed through `Redact` (defence in depth — tool args might carry a secret).

## J. Concurrent Dispatcher.Run × 2 race

- [x] J1. Two `Dispatcher`s sharing one store + one fakeCmux process N=10 pending rows; total NewWorkspace calls equal N (no row dispatched twice).
- [x] J2. After the parallel run, every row is in `running` and has a non-empty `ws`; the test passes under `go test -race`.
- [x] J3. New `store.TaskRepo.ClaimWorkspace(id, ws)` returns `(true, nil)` on the first claim and `(false, nil)` on a duplicate claim attempt against the same row. (The atomic claim is what makes J1/J2 pass.)
- [x] J4. A `ClaimWorkspace` failure (`claimed=false`) leaves the row's existing ws intact and the dispatcher releases any lock_key it acquired.

## Cross-cutting

- [x] All existing `internal/dispatch` and `internal/store` tests still pass.
- [x] `make test` (race detector clean) green.
- [x] `make lint` green (no new findings).
- [x] G6 (added post-design-review): fenceEscape is idempotent under repeated `<<<<` runs.
- [x] J5 (added post-design-review): NewWorkspace failure after ClaimWorkspace clears the `__dispatching__` sentinel.
- [x] H8 (added post-design-review): `Redact` godoc explicitly enumerates patterns NOT covered (AWS / Google / Stripe / raw JWT).
