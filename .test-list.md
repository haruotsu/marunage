# PR-83 GitHub Discovery source plugin — t_wada test list

> Drive Red → Green → Refactor in this order. Tick each entry when the matching
> Go test goes green. New cases discovered mid-implementation get appended at
> the bottom of the relevant section.

## A. Runner abstraction (internal/source/github/runner.go)

- [x] R1. `ExecRunner.Run("sh", "-c", "printf out; printf err >&2")` returns stdout="out", stderr="err", err=nil.
- [x] R2. `ExecRunner.Run("marunage-no-such-tool")` returns an error wrapping `exec.ErrNotFound`.

## B. Plugin.List (internal/source/github/github.go)

- [x] L1. `New()` without runner option uses ExecRunner default; `New(WithRunner(fake))` injects the test fake.
- [x] L2. List runs the documented gh subcommand: `gh search issues "is:open assignee:@me" --json ...` (issues) AND `gh search prs "is:open assignee:@me" --json ...` (PRs); fake runner records both invocations.
- [x] L3. List parses gh JSON into `Task`s with `ExternalID="<owner>/<repo>#<number>"`, `Title`, `Body`, `SourcePath=URL`, `Source="github"`, `Done=false`.
- [x] L4. List combines issue + PR results, preserving order (issues first, then PRs).
- [x] L5. Empty JSON array (`[]`) yields an empty task slice with no error.
- [x] L6. Malformed JSON returns `ErrInvalidResponse`.
- [x] L7. Runner non-nil error is wrapped and returned (not silently swallowed).
- [x] L9. Each task carries `RawMetadata["type"]` ∈ `{"issue","pr"}` and `RawMetadata["updated_at"]` (RFC3339).

## C. Plugin.Since (Sincer)

- [x] S1. `Since(ctx, "")` behaves like List — same query, no `updated:` qualifier.
- [x] S2. `Since(ctx, "2026-01-02T03:04:05Z")` appends `updated:>=2026-01-02T03:04:05Z` to the gh search query for both issues and prs.
- [x] S3. Adapter satisfies `source.Sincer` at compile time.

## D. Plugin.Complete (Completer)

- [x] C1. `Complete(ctx, "owner/repo#123")` invokes `gh issue close 123 --repo owner/repo`.
- [x] C2. `Complete` with `WithCompletionComment("done by marunage")` first invokes `gh issue comment ...`, then close.
- [x] C3. `Complete(ctx, "bad-format")` returns `ErrInvalidExternalID` without invoking the runner.
- [x] C4. Runner error from close propagates wrapped.
- [x] C5. Adapter satisfies `source.Completer` at compile time.

## E. Plugin.AuthStatus & Setup

- [x] A1. `AuthStatus` runs `gh auth status`; exit-zero → `AuthAuthenticated`.
- [x] A2. Non-nil runner error → `AuthNotConfigured`, no error returned.
- [x] A3. Missing `gh` binary → `AuthNotConfigured`, no error.
- [x] A4. `Setup` NonInteractive returns `ErrInteractiveSetupRequired` when not authed; no-op when authed.
- [x] A5. Interactive Setup unauthenticated returns hint error pointing at `gh auth login`.

## F. Adapter (internal/source/github/adapter.go)

- [x] F1. `Adapter` implements `source.Plugin`, `source.Sincer`, `source.Completer` at compile time.
- [x] F2. `Adapter.Name()` returns `"github"`.
- [x] F3. `Adapter.List` round-trips through inner Plugin.

## G. Manifest + RegisterBuiltin

- [x] G1. Embedded `plugin.toml` parses through `source.LoadManifestFromBytes` and declares capabilities `[list, setup, auth-status, since, complete]` with `sync_mode = "bidirectional"`.
- [x] G2. `RegisterBuiltin(r, opts...)` registers under name "github" and passes `source.ValidateAgainstManifest`.
- [x] G3. Double `RegisterBuiltin` returns `source.ErrPluginAlreadyRegistered`.

## H. Cross-cutting

- [x] H1. `make test` (race detector clean) green.
- [x] H2. `make lint` green.
- [x] H3. Existing tests still pass (no regressions in `internal/source/markdown` or other packages).
