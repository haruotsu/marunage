# PR-102 Reflection hook

あなたは marunage プロジェクトの **PR-102 Reflection フック** を実装する worker claude です。
このworktree (`marunage-pr102-reflection` ブランチ `feat/pr-102-reflection`) は本 PR 専用です。

## ゴール

`docs/pr_split_plan.md` の **PR-102 Reflection フック** セクションに記載の機能を `internal/dispatch/` の完了フックとして実装する:

- `done` 直後に **同一ws** へ reflection prompt を送る (cmux send 経由)
- `marunage-reflect` SKILL.md (`internal/skills/embedded/marunage-reflect/SKILL.md`) を新規追加
- DBスキーマ拡張: `tasks.reflection` カラム新設 (migrations 追加)
- サンプリング率設定: `[execution] reflection_sample_rate = 1.0` (0.0〜1.0、deterministic seedも可)
- 完了から reflection を集める非同期 goroutine。タイムアウト + cancel 対応

## 必須ルール

1. **CLAUDE.local.md / docs/requirement.md / docs/pr_split_plan.md を必ず読む**
2. **TDD (t_wada)**
3. **コードは英語**
4. **既存パターンを踏襲** — `internal/dispatch/dispatch.go` の functional options 流儀
5. **適切な粒度で commit**
6. **不要コメント禁止**
7. **既存テストを壊さない**
8. **PR-72 (triage) と internal/dispatch/ で衝突する可能性あり** — 新規ファイル `reflection.go` に分離して prompt.go の修正は最小に

## 重要な参考ファイル

- `internal/dispatch/dispatch.go`
- `internal/dispatch/prompt.go`
- `internal/cmux/cmux.go`
- `internal/store/tasks.go`
- `internal/store/migrations/`
- `internal/skills/`

## 作業フロー (完了まで自走)

1. ブランチ確認 (`feat/pr-102-reflection`)
2. テストリスト作成 (sampling, ws send, reflection persist, cancel)
3. Red → Green → Refactor
4. 複数 commit
5. `make test && make lint` 全緑
6. `git push -u origin feat/pr-102-reflection`
7. `gh pr create` — タイトル `feat(dispatch): PR-102 reflection hook + marunage-reflect skill`
8. **`/design-review`** Skill 実行 → 修正
9. **`/review-fix-loop`** Skill 実行 → 自動 fix
10. `gh pr comment <PR#>` で動作検証手順
11. 最後に **"DONE PR-102 #<PR_NUMBER>"** と echo

## このworktree
- path: `/Users/haruto/src/github.com/haruotsu/marunage-pr102-reflection`
- branch: `feat/pr-102-reflection`
- base: main (`307dcfd`)
- 他 11 PR が並列開発中。**特に PR-72 と internal/dispatch/prompt.go で衝突する可能性**。

頑張って。
