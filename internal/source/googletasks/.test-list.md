# PR-84 Google Tasks source — t_wada test list

## A. Plugin shape & interface compliance
- [ ] A1. Plugin.Name() == "googletasks"
- [ ] A2. *Plugin satisfies source.Plugin, source.Adder, source.Completer, source.Deleter at compile time

## B. AuthStatus
- [ ] B1. With no Client configured, AuthStatus returns AuthNotConfigured
- [ ] B2. With Client whose Ping succeeds, AuthStatus returns AuthAuthenticated
- [ ] B3. With Client whose Ping returns ErrUnauthorized, AuthStatus returns AuthRevoked

## C. List
- [ ] C1. With no Client, List returns ErrNotConfigured
- [ ] C2. With one tasklist containing two tasks, List returns 2 source.Tasks (one per tasklist task)
- [ ] C3. List propagates Source = "googletasks", ExternalID = upstream task id, Title = task title, Done = (status == "completed")
- [ ] C4. Notes flow into source.Task.Body
- [ ] C5. Multiple tasklists merge; SourcePath carries the tasklist id (tasklists/<id>)
- [ ] C6. List propagates ctx cancellation
- [ ] C7. List propagates client errors

## D. Add
- [ ] D1. With no Client, Add returns ErrNotConfigured
- [ ] D2. Add with empty title returns ErrInvalidTitle
- [ ] D3. Add inserts a task in the default tasklist and returns source.Task with non-empty ExternalID
- [ ] D4. Add forwards notes to the task's Notes field on the upstream object
- [ ] D5. Add propagates client errors

## E. Complete (mirror: marunage done -> Google completed)
- [ ] E1. With no Client, Complete returns ErrNotConfigured
- [ ] E2. Complete with empty externalID returns ErrInvalidTaskID
- [ ] E3. Complete with id present in default tasklist marks the upstream task status="completed"
- [ ] E4. Complete with id present in a non-default tasklist still works (search across lists)
- [ ] E5. Complete with unknown id returns ErrTaskNotFound
- [ ] E6. Complete propagates client errors

## F. Delete
- [ ] F1. With no Client, Delete returns ErrNotConfigured
- [ ] F2. Delete with empty externalID returns ErrInvalidTaskID
- [ ] F3. Delete with id present removes the upstream task
- [ ] F4. Delete with id present in a non-default tasklist still works
- [ ] F5. Delete with unknown id returns ErrTaskNotFound
- [ ] F6. Delete propagates client errors

## G. Setup
- [ ] G1. Setup with NonInteractive=true and no Client returns ErrNonInteractiveRequiresAuth
- [ ] G2. Setup forwards to the Authenticator (when wired) and refreshes Client on success

## H. Manifest / built-in registration
- [ ] H1. Embedded plugin.toml parses to name=googletasks, sync_mode=bidirectional, capabilities cover list/setup/auth-status/add/complete/delete
- [ ] H2. RegisterBuiltin succeeds and passes source.ValidateAgainstManifest
- [ ] H3. RegisterBuiltin twice returns ErrPluginAlreadyRegistered
