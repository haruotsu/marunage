# PR-84 Google Tasks source — t_wada test list

## A. Plugin shape & interface compliance
- [x] A1. Plugin.Name() == "googletasks"
- [x] A2. *Plugin satisfies source.Plugin, source.Adder, source.Completer, source.Deleter at compile time
- [x] A3. *Plugin deliberately does NOT satisfy source.Sincer (no upstream delta endpoint yet)

## B. AuthStatus
- [x] B1. With no Client configured, AuthStatus returns AuthNotConfigured
- [x] B2. With Client whose Ping succeeds, AuthStatus returns AuthAuthenticated
- [x] B3. With Client whose Ping returns ErrUnauthorized, AuthStatus returns AuthRevoked
- [x] B4. Non-auth Ping error propagates unchanged (transient outage != revoked credential)

## C. List
- [x] C1. With no Client, List returns ErrNotConfigured
- [x] C2. List with one tasklist + two tasks returns 2 source.Tasks (Source/ExternalID/Title/Body/Done/SourcePath all set)
- [x] C3. Multiple tasklists merge; SourcePath = "tasklists/<list-id>"
- [x] C4. List propagates ctx cancellation
- [x] C5. List propagates upstream errors

## D. Add
- [x] D1. With no Client, Add returns ErrNotConfigured
- [x] D2. Add with empty title returns ErrInvalidTitle
- [x] D3. Add inserts in default tasklist; ExternalID comes from upstream-confirmed value
- [x] D4. Notes flow into upstream Notes
- [x] D5. WithDefaultTaskList override is honoured
- [x] D6. Add propagates upstream errors

## E. Complete (mirror: marunage done -> Google completed)
- [x] E1. With no Client, Complete returns ErrNotConfigured
- [x] E2. Complete with empty externalID returns ErrInvalidTaskID
- [x] E3. Complete patches Status=completed in the default list
- [x] E4. Complete locates id in a non-default list
- [x] E5. Complete with unknown id returns ErrTaskNotFound

## F. Delete
- [x] F1. With no Client, Delete returns ErrNotConfigured
- [x] F2. Delete with empty externalID returns ErrInvalidTaskID
- [x] F3. Delete drops the upstream task
- [x] F4. Delete locates id in a non-default list
- [x] F5. Delete with unknown id returns ErrTaskNotFound

## G. Manifest / built-in registration
- [x] G1. Embedded plugin.toml parses to name=googletasks, sync_mode=bidirectional, capabilities cover list/setup/auth-status/add/complete/delete
- [x] G2. Embedded plugin.toml does NOT declare since
- [x] G3. RegisterBuiltin succeeds and passes source.ValidateAgainstManifest
- [x] G4. RegisterBuiltin twice returns ErrPluginAlreadyRegistered
- [x] G5. RegisterBuiltin with nil registry returns error

## H. GoogleClient (production wrapper)
- [x] H1. NewGoogleClient(nil http) returns error
- [x] H2. translateError maps 401/403 to ErrUnauthorized
- [x] H3. translateError passes other errors (incl. nil) through unchanged
