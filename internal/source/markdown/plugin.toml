[plugin]
name = "markdown"
version = "0.1.0"
description = "Markdown TODO file source (PR-50). Reads GitHub-flavoured task list lines and exposes them through the Discovery plugin contract."
sync_mode = "bidirectional"
capabilities = [
    "list",
    "setup",
    "auth-status",
    "since",
    "add",
    "complete",
    "delete",
]
